# =============================================================================
# Studio 项目 Conda 环境（与 requirements_backup.txt 配套）
# =============================================================================
#
# 覆盖范围：comment/（评论情感）、content/（BERTopic 主题）、
#           visualization/judge_server.py（开发时在线研判，由 Vite 可选拉起）。
#
# 在仓库根目录（本文件与 requirements_backup.txt 同级）执行：
#
#   conda env create -f environment.yml
#   conda activate studio
#
# 更新已有环境（改动了 yml 或 pip 依赖时）：
#
#   conda env update -f environment.yml --prune
#
# 前端仪表盘（visualization/）另需 Node.js >= 18 与 pnpm，见 visualization/README.md。
#
# =============================================================================
# GPU 说明
# - 默认安装 CPU 版 PyTorch（cpuonly），任意机器可解环境。
# - 若要用 NVIDIA GPU：删掉依赖里的 cpuonly，并取消下面「GPU 示例」一行的注释，
#   同时把 pytorch-cuda= 改成与你驱动匹配的版本（以 https://pytorch.org/ 为准）。
# - RTX 50 等新架构若稳定版 torch 不支持，请按 content/content_README.md 改用
#   PyTorch Nightly/CUDA 构建，并自行 pip 覆盖 conda 中的 torch 三件套。
# =============================================================================

name: studio

channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip

  # ----- PyTorch（conda；优先于 pip 装 torch，减少与 CUDA 二进制混搭问题）-----
  - pytorch>=2.2.0,<2.6
  - torchvision>=0.17.0
  - torchaudio>=2.2.0
  - cpuonly

  # GPU 示例：删除 cpuonly，并取消注释下面一行（版本号以 https://pytorch.org/ 为准）
  # - pytorch-cuda=12.4

  # ----- 其余 Python 包：走 pip + 项目备份清单（不含 torch）-----
  - pip:
      - -r requirements_backup.txt
