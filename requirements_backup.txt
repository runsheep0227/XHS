# =============================================================================
# Studio 项目 Python 依赖（可移植备份清单）
# =============================================================================
#
# 【使用方式】
# A）仅用 pip：先安装 PyTorch（见下方链接），再执行
#    pip install -r requirements_backup.txt
# B）用 Conda（推荐与本仓库 environment.yml 一起用）：在仓库根目录执行
#    conda env create -f environment.yml
#    其中已通过 conda 安装 CPU 版 PyTorch，本文件由 yml 的 pip 段自动安装。
# C）若仅跑 comment 或 content 子项目，可只装对应段落中的包（见文末分组）。
#
# 【PyTorch 官方安装命令】
# https://pytorch.org/get-started/locally/
# 纯 CPU 示例：
#    pip install torch torchvision torchaudio
# CUDA 12.x 示例（以官网当前命令为准）：
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# 【说明】
# - 本文件替代原先 conda/pip freeze 中带 file:// 的条目，保证 pip 可从 PyPI 拉取。
# - 版本号在「可安装」与「与当前代码兼容」之间折中；若需严格复现某次实验，可在新环境中
#   pip freeze > requirements-lock.txt 再单独存档。
# - 未将 torch 写入本列表，避免与 CUDA 变体冲突；请务必先装 torch 再装 transformers 等。
# =============================================================================

# --- 数值、表格与通用工具 ---
numpy>=1.26.4,<2.3
pandas>=2.2.0,<3.2
scipy>=1.11.0,<1.15
tqdm>=4.66.0

# --- 机器学习 / 传统 ML ---
scikit-learn>=1.5.0,<1.9

# --- HuggingFace：评论情感微调、推理、评估（comment/）---
# 依赖 torch（需预先安装）。accelerate 为 Trainer 常用可选加速项。
transformers>=4.44.0,<5.3
datasets>=3.0.0,<5.6
accelerate>=0.34.0,<1.13
safetensors>=0.4.0

# --- 大模型标注 OpenAI 兼容 API（comment/llm_annotate.py）---
openai>=1.40.0,<3

# --- 可视化（comment/evaluate_model.py、content/bertopic_visualize.py）---
matplotlib>=3.8.0,<3.12
seaborn>=0.13.0,<0.14

# --- 中文分词（content/clean_data.py）---
jieba>=0.42.1

# --- 主题建模链路（content/：bertopic_train.py、predict.py、bertopic_visualize.py）---
bertopic>=0.16.0,<0.18
sentence-transformers>=3.0.0,<5.3
umap-learn>=0.5.6,<0.6
hdbscan>=0.8.33,<0.9

# --- 可选：bertopic_visualize 词云图（未安装则跳过图 10）---
wordcloud>=1.9.3,<1.11

# =============================================================================
# 以下为「分组速查」（内容与本文件上文重复，勿与 pip -r 同时使用；仅作说明）
# =============================================================================
# [comment 最小集]
#   numpy pandas scipy tqdm scikit-learn torch(transformers官网先装)
#   transformers datasets accelerate safetensors openai matplotlib seaborn
#
# [content 最小集]
#   numpy pandas scipy tqdm scikit-learn torch(transformers官网先装)
#   jieba matplotlib seaborn bertopic sentence-transformers umap-learn hdbscan
#   wordcloud（可选）
#
# [本项目未在 .py 中直接 import、freeze 里曾出现的环境遗留包]
#   playwright、plotly、openpyxl、gensim、evaluate、peft 等可按需自行加入，非跑通 comment/content 主流程所必需。
# =============================================================================
