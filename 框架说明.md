# 小红书 AIGC 内容分析研究框架说明

> 本文档依据研究流程架构图，结合项目实际实现，对整体框架进行系统性说明。

---

## 一、研究背景与目标

本研究以小红书平台为数据来源，聚焦**AIGC（人工智能生成内容）**相关话题，构建一套完整的内容分析流水线，旨在：

1. 识别用户在小红书上讨论 AIGC 的**主题结构**（宏观分类与微观分布）；
2. 分析用户评论的**情感倾向**（正向 / 中性 / 负向三极性）；
3. 通过统计检验与可视化，探究**主题与情感之间的关联关系**。

---

## 二、整体框架概览

本框架分为三个主要阶段，构成端到端的研究流水线：

```
数据采集（关键词抓取 + 清洗）
    ↓
分析管道（主题分析 ‖ 情感分析，并行执行）
    ↓
关联分析与可视化（以 note_id 为关联键整合两路结果）
```

---

## 三、第一步：数据采集

### 3.1 数据来源

以 AIGC 相关关键词（见 `content/aigc_keyword.txt`）在小红书平台执行搜索抓取，原始数据分为两路并行采集：

- **笔记数据**：`rawdata/search_contents_YYYY-MM-DD.json`，字段含 `note_id`、`title`、`desc`
- **评论数据**：`rawdata/search_comments_YYYY-MM-DD.json`，字段含 `note_id`、`nickname`、`content`、`ip_location` 等

两路数据均以 **`note_id`** 作为贯穿全流程的唯一关联键。

### 3.2 笔记数据清洗（`content/` 模块）

| 步骤 | 脚本 | 核心操作 |
|------|------|---------|
| 一级清洗 | `clean_desc.py` | 字段抽取、去重（按 `note_id` 首次出现保留）、空内容过滤 |
| 二级清洗 | `clean_data.py` | 去表情占位 / 话题 / @符号 / 链接；jieba 分词；停用词过滤；长度门控（全文 ≥20 字符，分词 ≥10 词） |

**数据指标**：原始笔记 18,255 条，有效笔记 17,583 条（96.3%），含独立帖子 10,851 个。

### 3.3 评论数据清洗（`comment/` 模块）

| 步骤 | 脚本 | 核心操作 |
|------|------|---------|
| 一级清洗 | `clean_com.py` | 多日 JSON 合并、表情转文字、去 @、去重 |
| 二级清洗 | `clean_data.py` | 符号规范化、无意义词表过滤（`meaningless_word.txt`）、最终去重 |

**数据指标**：原始约 76,895 条，有效评论 75,245 条（留存率 97.85%）。

---

## 四、第二步：分析管道

两路清洗后的数据进入**并行分析管道**：左路对笔记执行主题分析，右路对评论执行情感分析。

---

### 4.1 主题分析（`content/bertopic_train.py`）

#### 4.1.1 技术路线——改进型 BERTopic 管道

本研究在标准 BERTopic 基础上引入**先验引导聚类**与**离群点回收**机制，流程如下：

```
文本向量化（BGE-large-zh-v1.5）
    ↓
UMAP 降维（n_neighbors=30, n_components=15, min_dist=0.1, metric=cosine）
    ↓
先验引导聚类（Guided BERTopic，seed_topic_list 来自 5 类宏观锚点关键词）
    ↓
HDBSCAN 密度聚类（min_cluster_size 动态计算：max(25, int(N/30×0.25)) ≈ 151）
    ↓
离群点回收（embedding 策略，reduce_outliers）
    ↓
c-TF-IDF 主题词提取（bm25_weighting=True, reduce_frequent_words=True, ngram=(1,2)）
```

**关键设计亮点：**
- **文本向量化**：采用 `BAAI/bge-large-zh-v1.5`（max_seq_length=512），相比通用 Sentence-BERT 在中文语义表示上更精准；
- **先验引导聚类**：通过注入 5 类宏观主题的锚点关键词列表，约束 HDBSCAN 聚类方向，避免纯无监督聚类偏离研究关注的 AIGC 语义空间；
- **动态 HDBSCAN**：`min_cluster_size` 随语料规模动态调整，防止固定阈值导致过度碎片化或欠分割；
- **离群点回收**：对 HDBSCAN 未归类的噪声点，通过嵌入相似度回收入最近主题，减少信息损失（噪声从默认约 15% 降至 3.7%）。

#### 4.1.2 微观 → 宏观主题映射

**微观主题**由 HDBSCAN 自动发现（共 **24 个**），**宏观主题**为人工预设的 5 类研究框架：

| 宏观主题 | 核心语义 | 映射置信度 | 笔记占比 |
|---------|---------|-----------|--------|
| AI 内容创作 | 绘画 / 视频 / 音乐 / 小说等生成与展示 | 0.710 | 30.2%（5,314 条） |
| AI 赋能工作生活 | 办公提效 / 副业 / 论文 / 代码辅助 | 0.745 | 20.5%（3,600 条） |
| AI 应用与测评 | 工具 / APP / 大模型 / 智能体测评 | 0.824 | 19.2%（3,380 条） |
| AI 学习教程 | 提示词 / 入门与进阶教程 | 0.843 | 17.4%（3,058 条） |
| AI 社会反思 | 版权 / 失业焦虑 / 伦理 / 深度伪造 | 0.951 | 12.7%（2,231 条） |

> **置信度规律**：语义边界越清晰（如"AI社会反思"话题词具体），置信度越高；内容创作话题词汇泛化，导致置信度相对偏低。

---

### 4.2 情感分析（`comment/` 模块）

#### 4.2.1 弱监督标注

- 从清洗后评论中随机抽取 **8,000 条**（`random_sample.py`）；
- 调用本地兼容 OpenAI API 的 LLM（LM Studio，默认 `http://127.0.0.1:1234/v1`）批量生成伪标签（`llm_annotate.py`），标签为 `-1 / 0 / 1`；
- 对标注结果执行约 **40% 人工审核**，矫正 LLM 噪声；
- 按 **8:1:1** 分层划分训练 / 验证 / 测试集（`split_dataset.py`，支持负样本过采样）。

**标签语义**：
| 标签 | 极性 | 语义 |
|------|------|------|
| `1` | 正向 | 赞美、期待、求带等积极情绪 |
| `0` | 中性 | 客观描述、工具性问题等 |
| `-1` | 负向 | 抵触、吐槽、反讽等消极情绪 |

#### 4.2.2 微调 MacBERT

- **基座模型**：`hfl/chinese-macbert-large`（可通过 `--model_name` 切换为 RoBERTa-wwm 等）；
- **训练设置**：支持类权重 / Focal Loss、早停、验证最优 checkpoint 导出；
- 训练中间产物保存于 `checkpoint_temp/`，最优模型导出至 `saved_model/`；

#### 4.2.3 性能校验

在 800 条测试集上评估：
- **Accuracy**：0.7438（README 记录 0.7450）
- **Macro-F1**：0.7354（README 记录 0.7384）
- 输出物：混淆矩阵、ROC/PR 曲线、雷达图、误判分析报告（`results/`）

#### 4.2.4 全语料库推理

- `predict_all.py` 对全量 **75,245 条**清洗评论批量推理；
- 支持断点续跑（`*.predict_ckpt.json`）；
- 输出 `comment_results/predicted_comments.json`（每条附加 `sentiment_class_id` / `sentiment_polarity` / `sentiment_text`）。

**情感分布结果**：
| 极性 | 占比（全量） | 占比（联表后） |
|------|------------|--------------|
| 正向 | 39.21% | 39.05% |
| 中性 | 35.28% | 35.42% |
| 负向 | 25.52% | 25.53% |

> 整体呈**积极偏态**，AIGC 相关内容在小红书平台总体获得正向反馈。

---

## 五、第三步：关联分析与可视化

### 5.1 以 note_id 为关联键的数据整合

两路分析结果通过 **`note_id`** 进行联表：
- 左表：`final_pro_topics.csv`（笔记维度，含宏观主题标签）
- 右表：`predicted_comments.json`（评论维度，含情感标签）
- 联表规模：68,886 条评论成功匹配到所属笔记的宏观主题

### 5.2 相关性分析检验

采用**卡方检验（χ² test）**检验主题类别与情感极性的统计关联：

| 统计量 | 数值 |
|--------|------|
| χ² | 1115.92 |
| 自由度（df） | 8 |
| p 值 | < 0.001 |
| Cramér's V | 0.0900 |

**结论**：主题与情感存在**统计显著关联**（p<0.001），但效应量较弱（V=0.09），说明宏观主题对情感极性具有一定解释力，但并非强决定因素，情感倾向更多受评论内容本身影响。

### 5.3 交互式仪表盘（`visualization/`）

基于 **React 18 + Vite + Recharts** 构建的前端可视化面板，运行于端口 **1306**：

| 模块 | 配色 | 展示内容 |
|------|------|---------|
| 主题分析页 | 粉色调 | 宏观主题分布饼图 / 柱状图；微观主题热力图；UMAP 降维散点 |
| 评论情感页 | 蓝青色调 | 情感极性分布；各主题下情感对比；按笔记聚合的评论热度 |
| 实时分析页 | 烟紫色调 | 联表统计、关联检验结果展示 |

---

## 六、技术架构总览

```
数据层        原始 JSON（小红书爬取）
                ↓
清洗层        Python 脚本（jieba / 规则清洗 / 去重）
                ↓
建模层        BERTopic（BGE + UMAP + HDBSCAN + c-TF-IDF）
              MacBERT-large 三分类（LLM 弱监督 + 人工审核微调）
                ↓
分析层        卡方检验 + Cramér's V（scipy / pandas）
                ↓
可视化层      React 18 + Vite + Recharts + TailwindCSS
```

**核心依赖**：

| 模块 | 主要依赖 |
|------|---------|
| 主题建模 | `bertopic`, `sentence-transformers`, `umap-learn`, `hdbscan`, `scikit-learn`, `jieba` |
| 情感分析 | `torch`, `transformers`, `datasets`, `openai` |
| 统计检验 | `scipy`, `pandas`, `numpy` |
| 可视化前端 | `react`, `vite`, `recharts`, `tailwindcss`, `react-router-dom` |

---

## 七、创新点与局限

### 创新点

1. **先验引导 + 无监督融合**：将研究框架的先验知识（5 类宏观主题锚点）注入 BERTopic，兼顾数据驱动与理论驱动；
2. **离群点回收机制**：显著降低噪声比例（3.7%），提升主题覆盖率；
3. **弱监督情感标注**：LLM 生成伪标签 + 人工审核，以较低成本构建高质量训练集；
4. **端到端联表设计**：以 note_id 贯穿全流程，实现笔记内容主题与评论情感的精确对齐。

### 局限

1. 爬取数据仅覆盖特定时间段，存在时效性限制；
2. 主题-情感关联效应量较弱（Cramér's V=0.09），解释力有限；
3. 情感模型准确率约 74%，三分类边界（尤其正向 vs 中性）存在混淆；
4. 弱监督标注引入的 LLM 噪声即便经人工校正，仍可能影响微调上限。

---

*文档生成时间：2026-05-16*  
*对应项目路径：`e:\document\PG\studio`*
